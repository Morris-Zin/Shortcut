typescript - better typing catch errors
considered python rejected cause going pwa route and if use python it introduce more complexity

pwa route confirmed cause wanna use on mobile, web, desktop 

style - functional programming style cause app contains transforming data 

stack minmal react typescript with local storage for now at least 

main challenge - algorithm right now can only think of brute force 
- needa optimize

strucruting of code 
- wanted to build an app like a plugin so can be used this logic in other apps 
but higher complexity for now, so if we build optimizer make it a standlone indepdent of the react


